# STRAT-M2-001 Closeout Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Close STRAT-M2-001 by summarizing the final amortized Duck findings, updating governance docs, and surfacing the σ-collapse limitation to users.

**Architecture:** All work happens in documentation: promote the stabilization evidence already captured under `plans/active/strat-m2-001/` and `plans/active/strat-m2-002/` into the strategy + fix-plan narratives, then add a Known Limitations entry referencing FND-M2-2026-01.

**Tech Stack:** Markdown (`docs/strategy/mainstrategy.md`, `docs/fix_plan.md`, `docs/user/known_limitations.md`), existing artifact directories, pytest smoke selectors for amortized workflows.

---

### Task 1: Strategy §7 close-out narrative

**Files:**
- Modify: `docs/strategy/mainstrategy.md`

**Step 1: Draft the "M2 Close-out" block**
- Under the existing M2 section, add a short subheading (e.g., “M2 Close-out — σ collapse documented 2026-01-29”) describing:
  - The canonical 150-iteration amortized run metrics (loss delta, σ stuck at 0.50°)
  - Scale-study table summary (20/50/100 image behavior)
  - Stabilization attempts (`clip_grad_norm`, curriculum) and their outcomes (NaN + σ frozen)
  - Direct references to `FND-M2-2026-01` and artifact roots `plans/active/strat-m2-001/reports/2026-01-29T151722Z/scale_study/` and `plans/active/strat-m2-002/reports/2026-01-29T235959Z/`

**Step 2: Note next actions**
- Add a short paragraph pointing readers to STRAT-M3-001 as the active refinement path and stating that amortized σ recovery is out of scope until a new likelihood/geometry plan exists.

**Step 3: Review diff**
```bash
rg -n "M2 Close-out" docs/strategy/mainstrategy.md
git diff -- docs/strategy/mainstrategy.md
```

### Task 2: `docs/fix_plan.md` status + FSM update

**Files:**
- Modify: `docs/fix_plan.md`

**Step 1: Mark STRAT-M2-001 as closed**
- Update the bullet list to show STRAT-M2-001’s status as **closed** (exit criteria met, limitation documented).
- Fold the stabilization summary into a final conclusion sentence that points to FND-M2-2026-01 and the STRAT-M2-002 artifacts.

**Step 2: Add the FSM line**
- Append a new `Supervisor state:` line such as `Supervisor state: focus=STRAT-M2-001 state=closed dwell=0 artifacts=plans/active/strat-m2-002/reports/2026-01-29T235959Z/ next_action=shift_focus_to_STRAT-M3-001`.
- Ensure dwell resets to 0 so the next focus selection starts fresh.

**Step 3: Review diff**
```bash
rg -n "STRAT-M2-001" docs/fix_plan.md
git diff -- docs/fix_plan.md
```

### Task 3: Known limitation entry for amortized σ collapse

**Files:**
- Modify: `docs/user/known_limitations.md`

**Step 1: Add new subsection**
- Introduce a “Amortized mosaicity collapse (Duck geometry)” section that summarizes:
  - Symptom: σ predictions stay at 0.50° and runs diverge beyond 300–500 iterations
  - Scope: Duck dataset (20–100 images, 64×64 detector, Poisson likelihood)
  - Mitigations tried (scale study, gradient clipping, curriculum) and that they failed
  - References to `FND-M2-2026-01` and the stabilization evidence directories

**Step 2: Provide reproduction commands**
- Embed the canonical amortized CLI command plus the grad-clip + curriculum commands so users can reproduce the limitation and understand why it is classified as known behavior.

**Step 3: Review diff**
```bash
rg -n "Amortized mosaicity" docs/user/known_limitations.md
git diff -- docs/user/known_limitations.md
```

### Task 4: Verification + staging

**Step 1: Run mapped tests**
```bash
KMP_DUPLICATE_LIB_OK=TRUE pytest tests/test_vi_mosaic.py -k "amortized or curriculum" -v
```
- Pass criteria: CLI + curriculum smoketests still pass because no code changed.

**Step 2: Stage changes**
```bash
git status -sb
```
- Ensure only the intended docs are staged before handing off for review/commit.

Plan complete and saved to `docs/plans/2026-01-29-m2-closeout.md`. Two execution options:
1. **Subagent-Driven (this session)** — run superpowers:subagent-driven-development with checkpoints per task.
2. **Parallel Session** — spin a new worktree/session and run superpowers:executing-plans across the tasks.

Which approach?
