This index captures previously documented bugs, conventions, and parity gaps so follow-on agents can recall the highest-impact findings without rereading the full archives.

| Finding ID | Date | Keywords | Synopsis | Evidence Pointer | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| BUG-C-001 | 2025-10-07 | c-bug, phi, rotation, state-carryover | The C code incorrectly uses the final φ rotation from the previous pixel for the first φ step (φ=0) of the current pixel. | [Link](docs/bugs/c-parity-001.md) | Active |
| BUG-C-002 | 2025-10-09 | c-bug, misset, radians, mosaic | The random misset branch feeds 90-degree inputs into `mosaic_rotation_umat`, so the helper builds rotation matrices capped at 90 radians and yields non-physical orientations. | [Link](docs/bugs/verified_c_bugs.md) | Active |
| API-001 | 2025-10-05 | cli, pix0_vector_mm, units, meters | The `-pix0_vector_mm` CLI flag is parsed as metres instead of millimetres, introducing a silent 1000× detector-origin error unless users pre-convert the inputs. | [Link](docs/bugs/c-cli.md) | Active |
| API-002 | 2025-10-21 | cli, pix0, beam-center, override | When custom detector vectors are supplied, specifying `-pix0_vector_mm` recomputes `Fbeam/Sbeam` and silently overrides any `-Xbeam/-Ybeam` geometry inputs. | [Link](docs/bugs/c-cli.md) | Active |
| CONVENTION-001 | 2025-01-09 | detector, convention, custom, offset | Specifying any detector-vector flag (e.g., `-twotheta_axis`) silently switches the calculation convention to CUSTOM, which removes the `+0.5` pixel offset from beam center calculations. | [Link](docs/architecture/undocumented_conventions.md) | Active |
| CONVENTION-002 | 2025-09-19 | detector, units, meters, parity | Detector geometry computations must remain in metres; running them in Angstroms drove 10⁹-scale pixel errors until the hybrid meter/Angstrom scheme restored correlation. | [Link](docs/debugging/detector_geometry_debugging.md) | Resolved |
| BUG-C-003 | 2025-10-09 | c-bug, logging, beam-center, units | The TRACE_C `beam_center_m` output divides metre values by 1000 yet still labels them as metres, giving misleading 5e-05 readouts. | [Link](docs/bugs/verified_c_bugs.md) | Active |
| CONVENTION-003 | 2025-09-17 | pivot, detector, sample, twotheta | The C implementation automatically switches to SAMPLE pivot mode when `-detector_twotheta`, `-Xclose/-Yclose`, or XDS convention inputs are present, even if `-pivot sample` is not set. | [Link](docs/architecture/undocumented_conventions.md) | Active |
| CONVENTION-004 | 2025-10-09 | vectorization, scattering, units, parity | Phase C traces showed the PyTorch scattering vector stayed in Å⁻¹ instead of C’s m⁻¹, creating the first divergence and a 10⁷× intensity error across pixels. | [Link](reports/2026-01-vectorization-parity/phase_c/20251010T061605Z/summary.md) | Active |
| CONVENTION-005 | 2025-10-09 | vectorization, fluence, scaling, parity | The vectorized fluence path mixes unit systems, producing a 10⁹× scaling error that wipes out physical signal until the conversion chain is corrected. | [Link](reports/2026-01-vectorization-parity/phase_c/20251010T061605Z/summary.md) | Active |
| CONVENTION-006 | 2025-10-09 | vectorization, lattice, normalization, sinc | The PyTorch `sincg` implementation omits the `Na×Nb×Nc` normalization present in C, so lattice factors are under-scaled by roughly two orders of magnitude. | [Link](reports/2026-01-vectorization-parity/phase_c/20251010T061605Z/summary.md) | Active |
| PERF-001 | 2025-10-10 | oversample, omega, edge, parity | Phase E0 concluded that reusing the last subpixel's solid angle, polarization, and absorption when oversample averaging is disabled induces edge-pixel bias that holds full-frame correlation at 0.721. | [Link](reports/2026-01-vectorization-parity/phase_e0/20251010T092845Z/summary.md) | Active |
| FND-GRAD-2026-01 | 2026-01-29 | gradient, slow, chunk, timeout, pytest | Slow-gradient tests (`@pytest.mark.slow_gradient`, ~845s on CPU) are isolated behind `--run-slow-gradient-chunk` / `NB_RUN_SLOW_GRADIENT=1` to prevent full-suite timeout breaches. Policy registered in `tests/conftest.py` via `pytest_addoption` + `pytest_collection_modifyitems`. | [Link](reports/2026-01-test-suite-refresh/phase_n/20260129T050759Z/sprint3/policy.md) | Resolved |
| FND-PERF-2026-01 | 2026-01-29 | perf, bandwidth, memory, threshold | AT-PERF-003 bandwidth ratio (2048 vs 512) ranges 0.555–0.705 on current hardware. Threshold set to 0.45 (~80% worst-case) via `BANDWIDTH_RATIO_THRESHOLD` constant, replacing legacy `*0.5` inline check. | [Link](reports/2026-01-test-suite-refresh/phase_n/20260129T051604Z/sprint4/bandwidth_stats.md) | Resolved |
| FND-PROB-2026-01 | 2026-01-29 | probabilistic, gradient, zero, mosaic, benchmark | Probabilistic kernel yields near-zero gradients (≈9e-22) for `mosaic_spread_deg` across default (64×64) and hi-res (128×128, 30Å cell, 0.5Å λ) geometries. The analytic Gaussian envelope σ ≫ |ΔQ| for all sampled pixels, making envelope ≈ 1.0 and its derivative ≈ 0. Speedup achieved was 2.1×, below the 4× target. **Diagnostics (STRAT-PROB-003):** mean |ΔQ|/σ = 2.3e-9, FD gradient = 0.0; root cause is unit mismatch (σ in m⁻¹ vs |ΔQ| in ų¹). | [Link](plans/active/strat-prob-003/reports/2026-01-29T065513Z/) | Active |
| FND-VI-2026-01 | 2026-01-29 | vi, poisson-elbo, benchmark, gradients, kl-annealing, observation-scale | Canonical VI benchmark (`scripts/benchmark_vi_mosaic.py --iterations 150`) shows Poisson ELBO loss plateauing around 7.3e2 while posterior sigma collapses to 0.15° (true 2.0°). KL dominates the loss and gradients on posterior parameters stay small, so VI underestimates mosaic spread and runs slower than MC baseline. **Task 7 diagnostics (25-iter, 32×32):** gradient norms are finite and non-zero (|∇μ|≈0.3–0.5, |∇ρ|≈0.8) but log-likelihood is nearly flat across iterations while KL decreases steadily, confirming the likelihood signal is weak relative to KL regularisation. σ_mean crawls from 0.5° → 0.78° after 25 iters (true=2.0°). **KL annealing (β-VAE, 25-iter, β=0.2→1.0 over 40 steps):** With `kl_weight` plumbing added to `poisson_elbo` and `LinearKLWeightSchedule` driving annealing, σ_mean climbs from 0.49° → 0.81° in 25 iters (β only at 0.68 by iter 24), vs 0.78° without annealing. Trend is monotonically increasing and still accelerating. CLI flags `--kl-weight-start/end/warmup-steps` available in both `vi_poisson_diagnostics.py` and `benchmark_vi_mosaic.py`. Evidence archived in `plans/active/strat-vi-001/reports/2026-01-29T082528Z/`. **Long-run results (Task 5):** 80-iter diagnostics (32×32) reached σ=1.25° but did not cross 1.5°; canonical 150-iter benchmark (64×64) collapsed to σ=0.148° despite β=0.2→1.0 warmup=120. KL annealing alone is insufficient — likelihood signal remains too weak at 64×64 resolution. Evidence: `plans/active/strat-vi-001/reports/2026-01-29T083412Z/`. **Observation Scale Bug (Poisson likelihood rescaling):** Root cause identified — C-default fluence (~1.26e28 photons/m²) produces enormous expected counts, making per-pixel Poisson log-likelihood differences negligible relative to KL. Fix: `poisson_sample_observations()` in `src/nanobrag_torch/vi/observation_utils.py` rescales intensities to a target fluence (default 1e13) and draws integer counts via `torch.poisson`. Both benchmark and diagnostics scripts now accept `--fluence` and `--observation-seed` CLI flags. Plan: `docs/plans/2026-01-29-vi-poisson-likelihood-rescaling.md`. | [Link](plans/active/strat-vi-001/reports/2026-01-29T082528Z/) | Active |
| FND-CWD-2026-01 | 2026-01-29 | cwd, pytest, infrastructure, tmp | During full pytest runs, worker processes may execute under `/tmp/pytest-*`, causing `Path(__file__).parent.parent`-based asset lookups to fail. Sprint 1 introduced `_get_repo_root()` (memoized via `Path(__file__).resolve().parents[1]`) and `ensure_repo_cwd()` gate in `tests/conftest.py`. All fixture/helper asset resolution now uses absolute paths from the repo-root helper. | [Link](reports/2026-01-test-suite-refresh/phase_n/20260129T045018Z/sprint1/analysis/path_checks.md) | Resolved |
