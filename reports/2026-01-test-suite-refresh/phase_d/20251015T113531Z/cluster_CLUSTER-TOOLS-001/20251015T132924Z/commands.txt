# CLUSTER-TOOLS-001 Reproduction Commands
# STAMP: 20251015T132924Z

# Targeted test reproduction
env KMP_DUPLICATE_LIB_OK=TRUE pytest -v \
  tests/test_at_tools_001.py::TestAT_TOOLS_001_DualRunnerComparison::test_script_integration

# Full test file (all 9 tests)
env KMP_DUPLICATE_LIB_OK=TRUE pytest -v tests/test_at_tools_001.py

# Check nb-compare availability
which nb-compare

# Manual nb-compare invocation (as per test)
python scripts/nb_compare.py --outdir test_comparison -- \
  -default_F 100 \
  -cell 100 100 100 90 90 90 \
  -lambda 1.0 \
  -distance 100 \
  -detpixels 64 \
  -floatfile test.bin
