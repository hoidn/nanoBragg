# Phase A Commands Log
# Timestamp: 20251010T131000Z
# Task: TEST-SUITE-TRIAGE-001 Phase A preflight

# Environment capture
python -m site > env.txt 2>&1

# PyTorch/CUDA environment
python - <<'PY' > torch_env.txt 2>&1
import torch
print(f"PyTorch version: {torch.__version__}")
print(f"CUDA available: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"CUDA version: {torch.version.cuda}")
    print(f"Device count: {torch.cuda.device_count()}")
    print(f"Device name: {torch.cuda.get_device_name(0)}")
PY

# Disk usage
df -h . | tee disk_usage.txt

# Pytest collection
env KMP_DUPLICATE_LIB_OK=TRUE pytest --collect-only tests -q > collect_only.log 2>&1
